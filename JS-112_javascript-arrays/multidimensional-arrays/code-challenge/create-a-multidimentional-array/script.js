/**
 * JS-112: JavaScript Arrays - Loop Through Arrays
 * 
 * Code challenge: Create A Multidimensional Array
 */
// Task 1: Declare a variable named coordinates and assign it an empty array literal.
const coordinates = [];
// Task 2: Add one array inside the coordinates array. The new nested array should contain two number values.
coordinates.push([1, 2]);
// Task 3: Add two more arrays to the coordinates array. The coordinates array should have 3 arrays inside it -- each of those arrays should hold two number values.
coordinates.push([3, 4]);
coordinates.push([5, 6]);

console.log(coordinates);
