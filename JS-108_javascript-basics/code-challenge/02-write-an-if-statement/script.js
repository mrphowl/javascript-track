// Challenge: Write an If Statement
// Create a conditional statement that tests if the isAdmin variable is true.
// If it's true, set the value of the message variable to "Welcome admin".
const isAdmin = true;
const isStudent = false;
let message;

if ( isAdmin ) {
  message = 'Welcome admin';
}
