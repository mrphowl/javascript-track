// Code challenge: Add an `else if` Clause
// Add an else if clause that tests if the isStudent variable is true.
// If it's true, set the value of the message variable to "Welcome student".
const isAdmin = false;
const isStudent = true;
let message;

if ( isAdmin ) {
  message = 'Welcome admin';
} else if ( isStudent ) {
  message = 'Welcome student'
}
