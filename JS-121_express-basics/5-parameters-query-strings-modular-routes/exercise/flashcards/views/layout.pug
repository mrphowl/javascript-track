doctype html
html(lang="en")
  head
    title Flash Cards
  body
    include includes/header.pug
    if name
      section#content
        h2 Welcome, #{name}!
        form(action='/goodbye', method='post')
          button.btn(type='submit') goodbye
    block content
    include includes/footer.pug
