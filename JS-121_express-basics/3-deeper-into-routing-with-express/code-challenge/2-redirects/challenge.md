# Redirect challenge

1. Add a form with a 'goodbye' button
    > views/index.pug
2. Create `/goodbye` route
    > app.js
3. Clear cookie in `/goodbye` handler
    > app.js
4. Redirect to `/hello`
    > app.js
