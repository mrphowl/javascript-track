extends layout

block main
  section.desk
    h1= `${error.message} - Status: ${error.status}`
    img.desk-items(src='/static/img/desk.svg')
    a.button-primary-small(href='/') Go Home
    if !currentUser
      |  #[a.button-primary-small(href='/login') Login]
    else
      |  #[a.button-primary-small(href='/sets') Change Card Set]
    .paper
      pre.stack-trace= error.stack
    .tear